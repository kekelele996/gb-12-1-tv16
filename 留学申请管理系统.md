## 项目主要需求

### 项目概述

构建一个面向留学中介机构和自助留学学生的留学申请管理系统，帮助用户管理目标院校信息、准备申请材料、跟踪申请进度。系统覆盖从选校规划、文书撰写到录取结果跟踪的全流程，提升留学申请的组织效率和成功率。

### 功能需求

1. **院校数据库**：维护全球主要院校信息（校名、国家、城市、排名、优势专业、申请截止日期、学费范围），支持按国家、排名区间、专业方向筛选院校，院校详情页展示申请要求（GPA、语言成绩、标化考试要求）
2. **申请项目管理**：学生创建申请项目（目标院校 + 申请专业 + 申请轮次），系统自动关联院校申请要求和截止日期，申请状态流转：规划中→准备材料→已提交→等待结果→已录取/已拒/候补
3. **文书管理**：为每个申请项目管理文书材料——个人陈述（PS）、推荐信（RL）、简历（CV）、Essay，支持在线编辑和版本管理（记录每次修改的版本历史），留学顾问可在线批注和反馈修改意见
4. **材料清单与进度跟踪**：每个申请项目生成标准化材料清单（成绩单、语言成绩单、在读证明等），学生逐项上传材料并标记已完成，系统显示整体材料准备进度百分比
5. **时间线管理**：为每个申请项目生成关键时间节点时间线（语言考试日期、文书截止、网申截止、面试日期、预计出结果日期），临近截止日期自动发送提醒通知
6. **顾问协作**：留学顾问与学生绑定后可查看学生所有申请项目，顾问可在文书上添加批注，顾问可为学生推荐选校方案（含推荐理由），学生和顾问之间通过站内消息沟通
7. **申请数据看板**：展示学生个人申请总览——已申请院校数量、各状态分布饼图、录取率统计、目标与实际对比，机构管理后台可查看所有学生的申请汇总数据

### 技术要求

- 前端：React + TypeScript，使用 Ant Design 组件库，Vite 构建工具
- 后端：Django 4.x 框架，Python 3.11 开发
- 数据库：PostgreSQL，使用 Django REST Framework
- 文件存储：MinIO 存储文书文件和申请材料
- 认证方式：JWT 认证
- 版本管理：使用 diff-match-patch 库实现文书版本对比
- 图表：前端使用 ECharts 绘制申请数据看板
- 通知：站内消息 + 邮件通知（临近截止日期提醒）

## 代码存放规则

这是前后端分离项目。

前端代码放在 `frontend/`：

页面，组件，路由,状态管理,API 请求,类型定义,样式和资源等放在对应的子文件夹

后端代码放在 `backend/`：

数据库脚本放在 `database/`。

## 部署要求

项目必须支持 Docker Compose 一键部署。

需要提供以下文件：

- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建，先构建再用 Nginx 托管）
- `backend/Dockerfile`：后端项目构建与运行镜像
- `frontend/nginx.conf`：Nginx 配置，处理前端路由和 API 反向代理
- `.env.example`：所有环境变量的示例文件，附注释说明

docker-compose.yml 要求：

- 所有服务使用命名容器，便于管理
- 数据库数据使用命名卷持久化
- 前端通过 Nginx 反向代理后端 API
- 服务间通过 Docker 内部网络通信
- 数据库服务配置 healthcheck，后端服务依赖数据库健康检查通过后再启动
- 暴露端口（避免与其他项目冲突）：前端 8012，后端 3012，PostgreSQL 5505，MinIO 9009/9010
- 支持通过 `docker compose up -d` 一条命令启动全部服务

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：

- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令、后端启动命令）
- 访问地址
- 技术栈
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：

- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容

